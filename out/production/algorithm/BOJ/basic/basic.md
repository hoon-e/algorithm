# Basic
## string

```java
// substring(int start) // 문자열 자르기
// substring(int start, int end) // 문자열 자르기
// Integer.valueOf() // 문자열을 정수로
// String.valueOf() // 정수를 문자열로
// Arrays.sort() // 배열 정렬
// Arrays.toString(char[] character) // 스트링으로 변환

// 배열 복사
// Arrays.copyOf(T[] src, int newLength)
// System.arraycopy(T src, int srcPos, T dest, int dstPos, int length)

// String 관련
// string.substring()
// string.indexOf / string.startsWith
```